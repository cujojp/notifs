// = = = = = = = = = = =
// Build configuration
// = = = = = = = = = = =
module.exports = {
  // Express config
  express: {
    _viewsDirectory: process.env.PWD + '/views',
    _asstesDirectory:  process.env.PWD + '/public'
  },
  // Source paths for gulp tasks
  srcPaths: {

  }
};
